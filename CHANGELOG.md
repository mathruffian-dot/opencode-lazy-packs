# 更新紀錄（CHANGELOG）

> 📌 **這份檔案是本 repo 唯一的版本紀錄。**
> v1 的版本號散在九個懶人包的標頭裡，改了這份忘了那份，最後有三處對不起來。
> v2 起：**要知道「這份東西是第幾版、改了什麼」，一律看這裡。** 各技能檔內不再各自宣告版本。

版本標示方式：以 Git tag 為準。
- 已發布的版本會打上 tag（例如 `v1`），可用 `https://github.com/mathruffian-dot/opencode-lazy-packs/tree/<tag>` 直接瀏覽。
- 還沒打 tag 的內容 = 開發中，隨時會變動。

---

## v2（開發中，尚未發布 tag）

三天研習的內容全部到齊後才會打上 `v2` tag。在那之前，`main` 分支的內容會持續變動。

### 新增

- **`01-connect-model` 連接免費模型（OpenCode Zen）**
  v1 最大的缺陷：照著懶人包把 OpenCode 裝完，其實**一句話都問不動**——因為從頭到尾沒有教怎麼接模型。老師以為自己裝壞了，其實是整份文件漏了最關鍵的一步。v2 補上這一包，並明講「#00 裝完不會回答你是正常的，請接著做 #01」。
- **`02-file-toolkit` 內部工具包**
  讓 agent 長出手腳：Python 文件處理（Word／Excel／PPT／PDF／圖片／QR／轉 Markdown）＋ 影音工具（yt-dlp／FFmpeg）＋ 語音（Edge-TTS）。裝完就能用一句人話請 agent 產獎狀、算成績、做簡報、抽影片音軌。
  （影音與語音兩段仍在擴充中，文件處理的部分已可使用。）

### 修正

- **`07-github` 修掉一個會在研習現場誤報的判定。**
  原本用 `git config --global --get-regexp credential` 驗證 `gh auth setup-git` 有沒有生效。
  問題是：Windows 用 winget 裝的 Git for Windows **內建 Git Credential Manager (GCM)，而它的設定寫在 system 層不是 global 層**。
  於是實測會出現「查詢結果是空的、但 `git push` 完全正常」——agent 照原本的規則會判 `GH_SETUP_GIT=FAIL`，
  一整班同時誤報，助教得一台一台去查一個不存在的問題。
  改法：驗證改用 `git config --show-origin --get-all credential.helper`（看所有層級），
  **gh／GCM／push 成功三者任一皆算通過**，並在成功訊號表明訂 `GH_SETUP_GIT` 不作為硬性 FAIL 關卡。
  同時修正「九成的 push 失敗都是漏了 `gh auth setup-git`」這句——有 GCM 的機器上，`git push` 是開瀏覽器授權，不是要帳號密碼。
- **`07-github` 修掉裝置碼登入的無限循環（現場實際發生過）。**
  症狀：老師打完 8 碼、瀏覽器顯示授權成功，但 agent 一直說「沒有收到驗證」，然後給出一組新的碼，週而復始。
  成因（實測 gh 2.89）：`gh auth login` 印出碼之後**不會結束**，它要持續輪詢 GitHub 才拿得到 token；
  而 agent 執行指令通常有逾時（常見 2 分鐘），**老師還在手機上打碼，`gh` 就已經被砍掉了**（退出碼 124）。
  於是授權其實成功了、卻沒有任何行程在等著收 token；agent 判定失敗 → 重跑 → **產生新碼、舊碼作廢** → 循環。
  改法：明訂 agent **不得在自己的 shell 裡執行 `gh auth login`**，改為
  ① Windows 用 `Start-Process` 開一個不受逾時影響的獨立視窗，或 ② 請使用者自己開終端機；
  然後 agent **只輪詢 `gh auth status`**（exit 0 = 成功、1 = 尚未完成），**明確禁止重跑 login**。
  常見坑另補三條，包含「八碼怎麼打都說錯 ＝ 看到的是被重跑作廢的舊碼」。
- **`07-github` 套用同一套「該信誰」框架。**
  與 `06-supabase` 一致：`gh` 的指令與輸出、GitHub 網頁的介面一律以 `--help`、官方文件與現場畫面為準；
  「兩道門」、裝置碼會被逾時砍掉、註冊拼圖是風險評分、credential helper 查不到不等於沒設好、
  以及常見坑那張症狀對照表，才是這份文件不可取代的價值。
  並點明本包最容易搞砸的三件事**全部都是「多做了什麼」**——重跑 `gh auth login`、
  在自己的 shell 跑互動指令、看到 `--global` 查不到就叫使用者去修。**看到卡住，先停、先問。**

- **`06-supabase` 開頭新增「給 AI agent：這份文件該怎麼用」。**
  現場檢討的結論：今天的失敗不是文件太長，是**文件講錯了**（那句「不再需要 PAT」把唯一穩定的路封死），
  外加**缺少症狀對照**（`project_ref` 打錯會偽裝成憑證問題）。
  所以新增一段明確的使用原則，告訴 agent **哪些內容該信這份文件、哪些該以現況與官方文件為準**：
  UI 畫面與 API 用法一律以現場／官方為準；紅線、陷阱、症狀對照才是這份的價值。
  並明訂三條執行原則——先查現況再動手、失敗時先報錯誤原文然後停下來（**尤其不要換憑證**）、
  文件與現況對不上時以現況為準並回報差異。
  1-1／1-2 的 UI 導覽同步改寫為「以你眼前的畫面為準」，降低介面改版造成的誤導。

- **`07-github` 依現場回饋調整裝置碼登入的等待策略。**
  現場實際發生「重複三次認證才成功」——那不是重試三次終於通了，而是**第三次才跑贏 agent 的逾時**。
  輪詢上限由 5 分鐘改為 **10 分鐘**，並要求 agent **每分鐘回報一次進度**
  （沒有回報的話，使用者會以為當掉而自己重跑 `gh auth login`，反而作廢目前這組碼）。
  常見坑新增「當天現場才註冊新帳號的人登入特別慢」——全新帳號的第一次登入常夾著信箱驗證、
  裝置確認、甚至被要求當場設 2FA，這些都要塞進授權時間裡。
  2-1 同步補上「註冊完順便在瀏覽器登入一次」，把這些一次性關卡挪到前一晚。
- **`07-github` 補上註冊階段的拼圖驗證（Arkose FunCaptcha）。**
  原本只寫了「到 github.com/signup 自己註冊」，也只在常見坑提到**登入**階段的壅塞。
  實際上更難救的是**註冊**：它是風險評分不是解題，**同一個 IP 短時間多人註冊**會被判定為機器人批次開帳號，
  於是出現「解了十題還在解」而且解對也沒用的循環。
  新增「**一定要前一晚在家裡註冊**」的明確指示，以及現場救援順序（手機熱點 → 關 VPN → 無痕／換瀏覽器 → 不要猛按重試）。
- **`00-env-setup` 補上 GCM 說明**：裝 Git for Windows 會一併裝 GCM，並點明它的設定在 system 層、`git config --global` 查不到。

### 改寫

- **`07-github` 改成三階段教學**：①拿別人的東西（`git clone`，不用帳號）②怕弄丟（建 repo → push）③換電腦驗收（`git pull`）。每一階段對應老師的一個痛點，在他「已經痛過」之後才教，不再一次倒完。
  同時**移除 `gh repo delete`、`rm -rf` 等高風險內容**——研習現場照著貼、貼錯一個字就回不來的指令，不該出現在給新手的文件裡。
- **`08-workflow-skills` 開工／收工／初始化改成完整版**：v1 只有骨架（大致說了要做什麼），v2 是可以直接寫進 `~/.config/opencode/skills/` 的完整技能內容，並加上三層級自動偵測（本地／＋GitHub／＋Obsidian），裝到哪一層就做到哪一層。

### 修正

- **Git 與 GitHub CLI 到底誰負責安裝**：v1 的 #00 寫「Git／gh 請看 #02」、#02 又寫「Git 沒裝請先跑 #00」，兩份互踢皮球，結果**兩份都沒有真的安裝**。v2 起一律由 `00-env-setup` 負責安裝，`07-github` 只做登入與驗證。
- **Windows PowerShell 的 `&&`**：`node --version && npm --version` 這種寫法在 Windows PowerShell 5.1 會直接語法錯誤（不是指令失敗，是連跑都跑不起來）。全數改用 `;` 分隔。
- **Node.js 不是 OpenCode 的前置條件**：v1 把「先裝 Node.js」寫成裝 OpenCode 的必要步驟。實際上 OpenCode 是獨立執行檔，沒有 Node 也能跑；Node 是**後面那些 MCP 外掛**才需要。順序寫錯會讓老師卡在第一步。
- **專案藍圖檔名統一為大寫 `AGENTS.md`**：寫成小寫 `agents.md` 在 Windows 看起來一切正常，一到 Mac／Linux 就會發生「檔案明明在，agent 卻讀不到」的鬼打牆。
- **技能檔裡內嵌的 frontmatter 沒有用程式碼圍籬包住**：導致 agent 照抄時把外層文件的 `---` 一起吃進去，產生格式壞掉的技能檔。v2 一律用圍籬包住，並明講「不要把圍籬本身寫進檔案」。
- **移除公開 repo 裡殘留的個人資料夾路徑**：改成 `<vault名稱>` 這類佔位寫法。

### 結構調整

- **收斂為單一真相來源**：v1 同一份內容有兩份——根目錄的長版 MD（`00-環境建置.md` 之類）和 `skills/` 底下的短版雙胞胎，改了一邊忘了另一邊。v2 全部內容只放在 **`skills/<名稱>/SKILL.md`**，根目錄不再放教學 MD。
- **依「作品階梯」重新編號**：地基 → 內部工具 → 外部工具 → 資料庫 → API → 跨電腦。編號現在代表**建議的學習順序**，不只是流水號。
- **`README.md` 不再維護版本欄**：版本一律看本檔；懶人包清單與一鍵安裝的 skill 名合併成同一張表，避免兩張清單各自漂移（v1 就是這樣一處寫 10 個、一處寫 9 個）。
- **移出主線到 `extras/`**：Firebase 與瀏覽器控制。它們仍然有用，但不是三天研習的主線，也不是零基礎老師的第一站。
- **移除生圖包**：v1 的生圖需要自費的 OpenAI API 金鑰，違反「研習內容不預設老師要刷卡」的原則。改為在 README 指向免金鑰的替代 repo。
- **移除 `09-install-all`**：一次全裝改由根目錄 `SKILL.md` 的互動式流程處理，不再多維護一個只是「依序跑其他包」的技能。
- **重寫 `AGENTS.md`**：原本放的是維護者自用的「三倉同步規則」，那是內部筆記，不該出現在公開 repo。改寫成單純給 AI agent 看的 repo 導覽。

### 待補（v2 tag 之前要完成）

全部十一包已到齊。v2 可發布。

最後補齊的兩包：
- **`09-groq-api`**：語音轉字幕 ＋ 文字清洗。免費方案**不需要信用卡**（已對照官方 Billing FAQs 查證）。
  推薦 `whisper-large-v3` 而非 turbo——免費方案兩者額度完全相同，turbo 的優勢只有價格，對免費用戶等於零。
  最大的坑：Groq 的 `response_format` **沒有 `srt`**（OpenAI 有），填了回 400，所以本包附了自己組 SRT 的程式。
- **`10-netlify`**：網頁部署 ＋ 用 Functions 把 API 金鑰藏在後端。
  核心紀律是**先草稿再正式**——Netlify 2025-09 起改點數制，正式部署一次 15 點、每月 300 點，用完全站下線。

---

## v1（2026-05-19 ～ 2026-07-09）

> 完整內容保存在 `v1` tag：<https://github.com/mathruffian-dot/opencode-lazy-packs/tree/v1>
> 已經照 v1 做完、東西都能跑的人，**不需要急著重做**；想知道 v2 改了什麼、要不要跟進，看上面那一段就好。

第一版，九份懶人包：環境建置、NotebookLM、GitHub、Obsidian、第二大腦設定、Firebase、瀏覽器控制、開工／收工技能、生圖。定位是「把 Claude Code／Codex 的懶人包翻譯成 OpenCode 版本」，主要差異在設定檔格式與 MCP 的加法。

期間的主要變動：
- 2026-05-19　初版釋出（九份懶人包）
- 2026-05　　　一度新增「連接 Gemini」，隨後移除（見下方已知限制）
- 2026-06　　　加入 `skills/` 目錄與 `npx skills add` 一鍵安裝、新增 repo 根目錄的安裝入口 `SKILL.md`
- 2026-06-12　NotebookLM 包更新（改用 uv、補路徑陷阱警告與復原流程）
- 2026-07-09　v1 內容凍結，開始 v2 重構

### v1 的已知限制（也就是 v2 要修的東西）

1. **裝完不能用**：沒有任何一包教怎麼連接模型。
2. **Git／gh 沒有人真的安裝**：#00 和 #02 互相轉介。
3. **同一份內容有兩份**：根目錄長版 MD 與 `skills/` 短版，內容會漂移。
4. **版本號散落各處**：九個檔案各自宣告版本，已經對不起來。
5. **Windows 指令會語法錯誤**：`&&` 在 PowerShell 5.1 跑不動。
6. **前置條件寫錯**：把 Node.js 當成 OpenCode 的必要前置。
7. **有需要自費才能用的內容**：生圖包需要 OpenAI API 金鑰。
8. **曾有借用他人訂閱配額的路線**：v1 一度收錄以 OAuth 借用個人訂閱配額來驅動 agent 的「連接 Gemini」包，後來整包移除。這類做法可能違反服務條款、也會讓老師在不知情的狀況下踩線，**v2 起明確不收**。
